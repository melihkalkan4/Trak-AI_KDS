# Artifact Manifest — Paper 1 (Spatial vs Temporal Generalization)

> Phase 0 inventory. Every path below was **opened and inspected** (schema + row
> counts verified, not assumed). All paths are **read-only inputs** relative to repo
> root `C:\Users\Melih Kalkan\Desktop\Trak-AI_KDS`. Nothing here is modified.
>
> Inventory date (UTC): 2026-06-20T15:58Z · Branch: `paper1`

## 0. Status legend
- ✅ FOUND & verified · ⚠️ found under a different name than the master prompt expected · ❌ not present (→ recompute-or-report)

---

## 1. Core CV results (the heart of Paper 1)

Pipeline ÇP-2.5 ("cp25") under `reports/cp25/`. Each layer = feature tier; each CV
scheme = generalization regime. **All deterministic, SEED=42.**

| Prompt name | Actual path | Rows (excl. header) | Key columns |
|---|---|---|---|
| `master_comparison.csv` ✅ | `reports/cp25/12_master_comparison.csv` | 8 | layer,model,crop,n,r2_loyo,rmse_loyo,ss_loyo |
| `layer_a_LOYO_LOILO_spatio.csv` ⚠️→ | `reports/cp25/05_layer_a_results.csv` | 30 | r2,rmse_kg_da,mae_kg_da,mape_pct,bias_kg_da,ss_vs_b0,crop,cv,model,n,elapsed_s |
| `layer_b_results.csv` ✅ | `reports/cp25/06_layer_b_results.csv` | 30 | same schema as Layer A |
| `layer_c_results.csv` ✅ | `reports/cp25/07_layer_c_results.csv` | 32 | same schema (+stacking model, LOYO only) |
| `baselines_results.csv` ⚠️→ | `reports/cp25/02_baselines.csv` | 8 | crop,model,n,r2,rmse_kg_da,mae_kg_da,mape_pct,bias_kg_da,skill_score_vs_B0 |

CV schemes present in each layer file: **LOYO** (year groups, temporal), **LOILO**
(ilce_id groups, spatial), **Spatiotemporal** (5 year-blocks × 5 KMeans space-clusters = 25 blocks).
Models per layer: pls, elastic_net, random_forest, xgboost, gpr (+ stacking in Layer C, LOYO only).

### Sample sizes (verified)
- Layer A (climate-only): bugday **n=589**, aycicegi **n=576** (full TÜİK panel with climate).
- Layer B (+NDVI) & Layer C (+NDVI+soil): bugday **n=213**, aycicegi **n=209** (restricted to Sentinel-2 NDVI availability, 2017+).

---

## 2. Per-sample (out-of-fold) prediction artifacts → enable bootstrap CIs

Schema (all): `ilce_id,ilce,il,year,crop,verim_kg_da,yield_pred_loyo,abs_error`.
These hold the **LOYO out-of-fold predictions of each layer's LOYO champion model**.

| Prompt name | Actual path | Rows | Notes |
|---|---|---|---|
| `loocv_bugday.csv` ⚠️→ | `reports/cp25/05_loocv_predictions_bugday.csv` | 589 | Layer A wheat (champion=elastic_net) |
| `loocv_aycicegi.csv` ⚠️→ | `reports/cp25/05_loocv_predictions_aycicegi.csv` | 576 | Layer A sunflower (champion=random_forest) |
| (layer B wheat) ✅ | `reports/cp25/06_loocv_predictions_bugday.csv` | 213 | |
| (layer B sunflower) ✅ | `reports/cp25/06_loocv_predictions_aycicegi.csv` | 209 | |
| (layer C wheat) ✅ | `reports/cp25/07_loocv_predictions_bugday.csv` | 213 | |
| (layer C sunflower) ✅ | `reports/cp25/07_loocv_predictions_aycicegi.csv` | 209 | |
| `02_baseline_loyo_predictions.csv` ✅ | `reports/cp25/02_baseline_loyo_predictions.csv` | 1165 | per-sample B0/B1/B2/B3 predictions, both crops |

> NOTE: only **LOYO** per-sample predictions are persisted by the original pipeline.
> LOILO / Spatiotemporal per-sample predictions are **not** on disk → must be
> regenerated by deterministic inference re-run (see decisions.md D-04).

`loocv_results.json` ❌ not found (the per-sample CSVs above supersede it).
`reports/cp25_loocv_predictions_bugday.csv` (21 rows) = OLD province-level (il) v1 artifact — **not used** (district-level v2 is the thesis dataset).

---

## 3. Feature importance (XAI)

| Prompt name | Actual path | Rows | Columns |
|---|---|---|---|
| `shap_perm_layer_a_bugday.csv` ⚠️→ | `reports/cp25/08_perm_importance_A_bugday.csv` | 14 | feature,imp_mean,imp_std |
| `shap_perm_layer_a_aycicegi.csv` ⚠️→ | `reports/cp25/08_perm_importance_A_aycicegi.csv` | 14 | " |
| (layer B/C, both crops) ✅ | `reports/cp25/08_perm_importance_{B,C}_{bugday,aycicegi}.csv` | 14–23 | " |
| SHAP (figures only) ✅ | `reports/cp25/fig_shap_summary_{A,B,C}_{crop}.png` | — | beeswarm plots |

---

## 4. Model metadata / champion contract

| Prompt name | Actual path | Notes |
|---|---|---|
| `champion_metadata.json` ✅ | `models/cp25/champion_metadata.json` | champions: bugday=B0/Climatology (R²=0.213), aycicegi=C/gpr (R²=0.386); H1–H5 verdicts |
| `feature_names.json` ✅ | `src/cp2_model/feature_names.json` | LSTM (NDVI) feature names — prospective pipeline |
| `yield_meta_wheat.json` ✅ | `src/cp2_model/yield_meta_wheat.json` | |
| `yield_meta_sunflower.json` ✅ | `src/cp2_model/yield_meta_sunflower.json` | |
| `training_results.json` ✅ | `src/cp2_model/training_results.json` | cp2 training-era metrics |
| Frozen CV model bundles ✅ | `models/cp25/{baselines.pkl, layer_a_*.pkl, layer_b_*.pkl, layer_c_*.pkl, champion_aycicegi.pkl}` | NOT retrained; not loaded for fitting |

Note: there is no `champion_bugday.pkl` — consistent, because the wheat champion is the
B0 climatology baseline (lives in `baselines.pkl`), not an ML artifact.

---

## 5. Methodology / honesty documents

| Prompt name | Actual path | Content |
|---|---|---|
| `FLOV_METHODOLOGY.md` ✅ | `docs/FLOV_METHODOLOGY.md` | Forward-Looking Operational Validation; frozen-artefact contract; per-stage phenology; persistence baseline; Wilcoxon |
| `MULTIMODAL_VALIDATION_METHODOLOGY.md` ✅ | `docs/MULTIMODAL_VALIDATION_METHODOLOGY.md` | (to read in docs phase) |
| `persistence_baseline_durustluk.md` ⚠️→ | embedded in `reports/cp25/02_baselines.md` + FLOV §9 | B0–B3 defs; persistence as benchmark |
| `morans_i_spatial.md` ⚠️→ | `reports/cp25/11_spatial_diagnostics.md` | Moran's I results (wheat I=+0.257 p_sim=.011; sunflower I=+0.117 p_sim=.079) |
| `persistence_vs_model_wilcoxon.md` ⚠️→ | FLOV §9 + prospective `*_validation_summary.json` (wilcoxon block) | |
| `REPRODUCIBILITY.md` ✅ | `docs/REPRODUCIBILITY.md` | env + build recipe |
| extra ✅ | `docs/MOCK_DATA_AUDIT.md`, `docs/TEZ_RAPORU_FINAL.md`, `reports/cp25/13_loilo_mape_bootstrap_bugday.md` | honesty audit; final thesis report; existing LOILO MAPE bootstrap |

---

## 6. Pipeline source (read-only; reused for deterministic inference)

`src/cp25/` — fully reproducible, SEED=42 throughout:
- `02_baselines.py` (B0–B3) · `05_layer_a_climate_only.py` · `06_layer_b_climate_ndvi.py` · `07_layer_c_full.py`
- `08_xai_analysis.py` · `11_spatial_diagnostics.py` · `12_final_synthesis.py`
- Inputs consumed: `data/processed/calibration_features_layer{A,B,C}.csv`, `data/external/tuik/ilce_coords.csv`

Feature tiers (verified from source):
- **Layer A** (14): gdd_cum_season, gdd_flowering, vernalization_days, tp_season_sum, tp_winter_sum, tp_flowering, tp_grain_fill, aridity_index, heat_stress_days, t2m_flowering_mean, t2m_flowering_max, tdiff_mean, ssr_flowering_sum, ssr_season_sum
- **Layer B** = Layer A + NDVI (ndvi_max, ndvi_mean_season, ndvi_integral, …)
- **Layer C** = Layer B + soil (clay/sand/silt 0-5cm, …)

Model configs (verified, identical across layers): PLS(n_comp=3), ElasticNet(α=1.0,l1=0.5),
RandomForest(300,max_depth=5), XGBoost(200,max_depth=4,lr=0.05), GPR(Matern ν=2.5 + WhiteKernel, normalize_y),
Stacking(RF+XGB+GPR→Ridge(α=1.0)). Scaler applied to {pls, elastic_net, gpr, stacking}.

`src/prospective_validation/` — FLOV walk-forward inference (frozen LSTM+XGB, hash-locked).

---

## 7. Datasets (read-only)

| Path | Rows | Role |
|---|---|---|
| `data/processed/master_feature_matrix_2017_2024.csv` | (master) | **CORE INPUT** — 2017–2024 ilçe-year feature matrix |
| `data/yield/trakya_yield_2017_2024.csv` | | yields |
| `data/external/tuik/tuik_ilce_yields_clean.csv` | 1165 | TÜİK district yields 2004–2025 |
| `data/processed/calibration_features_layerA.csv` | 1165 | Layer A model input |
| `data/processed/calibration_features_layerB.csv` | 422 | Layer B input (NDVI subset) |
| `data/processed/calibration_features_layerC.csv` | 422 | Layer C input (NDVI+soil subset) |
| `data/external/tuik/ilce_coords.csv` | 34 | **REAL** 29-district lat/lon centroids (used by Spatiotemporal CV + Moran's I) — NOT the placeholder EVR coords |
| `data/processed/soil_ilce.csv` | | SoilGrids per-district |

---

## 8. Prospective (FLOV) — 2025/2026 forward validation

`reports/prospective/` — sites EVR_01…EVR_05, years 2025 & 2026.
- Full validation only for **EVR_01** (others have predictions + yield.json only).
- `EVR_01_{2025,2026}_validation_summary.json` — overall model vs naive-persistence + Wilcoxon.
- `EVR_01_{2025,2026}_validation_per_stage.csv` — phenology-stage R²/MAE/RMSE (**senescence collapse**).

**Coordinate status (verified, `src/prospective_validation/config.py` L105-109):** the 5 EVR
sites are **placeholder/approximate pilot coordinates** near Vize (≈41.0°N, 27.2°E), explicitly
flagged "to be replaced with real parcel coordinates before deployment" (FLOV §4). EVR_01 =
"Kendi tarlam — Vize merkez", 12.5 da (1.25 ha). → prospective results carry an explicit
placeholder-coordinate limitation. **Core LOYO/LOILO analysis does NOT depend on these.**

---

## 9. Existing figures (candidate reuse)

`reports/cp25/`: fig_baseline_comparison, fig_layer_a_comparison, fig_morans_i,
fig_correlation_matrix, fig_spatial_yield_map, fig_yield_vs_year, fig_yield_distribution,
fig_shap_summary_{A,B,C}_{crop}, fig_pdp_*, fig_uncertainty_calibration_*.
`docs/plots/`: wheat/sunflower prediction_vs_actual, *_xgb_shap_summary, advanced_eda 2017-2024.

---

## 10. Environment (reproduction fidelity)

Using the **existing repo venv** `venv/Scripts/python.exe` (Python 3.13.2) — the same
interpreter/versions that produced the original results (decisions.md D-02). Full freeze:
`repro/requirements_full_venv.txt` (257 pkgs). Key: numpy 2.4.3, pandas 3.0.1,
scikit-learn 1.8.0, xgboost 3.2.0, scipy 1.17.1, matplotlib 3.10.8, esda 2.9.0,
libpysal 4.14.1, shap 0.51.0. (statsmodels absent — not required; scipy covers Wilcoxon.)
