#pragma once
/*
 * TRAK-AI Rover — secrets.template.h
 *
 * Bu dosya bir ŞABLONDUR ve depoya commit edilir (gerçek değer içermez).
 * Kullanım:
 *   1. Bu dosyayı aynı klasöre "secrets.h" adıyla kopyalayın.
 *   2. secrets.h içindeki değerleri kendi gerçek bilgilerinizle doldurun.
 *   3. secrets.h .gitignore'ludur; ASLA commit etmeyin.
 *
 * config.h bu dosyayı (secrets.h) #include eder.
 */

#define WIFI_SSID       "YOUR_WIFI_SSID"
#define WIFI_PASSWORD   "YOUR_WIFI_PASSWORD"
#define OTA_PASSWORD    "YOUR_OTA_PASSWORD"
